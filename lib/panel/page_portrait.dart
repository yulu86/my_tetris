import 'package:flutter/material.dart';

class PagePortrait extends StatelessWidget {
  const PagePortrait({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('PagePortrait'),
    );
  }
}
